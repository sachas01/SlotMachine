{{>head}}

  <div class="row">
    <div class="col-lg-12 col-sm-12 col-12">
      <h1>Your Profile</h1>
    </div>
    <div class="col-lg-12 col-sm-12 col-12">
      <p>To play in any of the games on {{name}}, you will need to have bitcoin in your account to play with.  {{^user}}If you wish to deposit bitcoin to account, generate a bitcoin address below.{{/user}}<p>
    </div>
    <div class="col-lg-12 col-sm-12 col-12">
      <p><strong>Access to your account is controlled by an access key.  To get access to your account, bookmark this url: <div class="well"><a href="{{url}}/?key={{key}}">{{url}}/?key={{key}}</a></div></strong></p>
    </div>
    {{^user}}
    <div class="col-lg-12 col-sm-12 col-12">
      <p><a href="/profile/generate" class="btn btn-default">Generate Deposit Address</a></p>
    </div>
    {{/user}}
    {{#user}}
    <div class="col-lg-12 col-sm-12 col-12">
      <p>Your Deposit Address: <strong>{{address}}</strong></p>
      <p>Your Account Balance: <strong>{{balance}}</strong></p>
    </div>
    {{/user}}
  </div>

{{>foot}}